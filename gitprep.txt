==========
> grvu (final review update)
> gcm
> mint update
    (or gplr)
> gco <branch>
> grbim (git rebase -i master)
    %s/^pick/f/g
> grvd (git review dcommit)
> gcm
> git merge <branch>
> mint submit
    (or git push origin master)
==========
